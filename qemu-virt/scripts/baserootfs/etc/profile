# /etc/profile: system-wide .profile file for the Bourne shells

echo -n "hello Linux arm64 cortex-a53"
echo

hostname pzx

PATH=/bin:/sbin:/usr/bin:/usr/sbin
LD_LIBRARY_PATH=/lib:/lib64:/usr/lib

export PATH LD_LIBRARY_PATH